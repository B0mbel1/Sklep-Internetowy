@model IEnumerable<Film>

<ul class="nav nav-pills flex-column">
    @foreach (var film in Model)
    {
        <li class="nav-tem">
            <a class="nav-link" href="@Url.Action("Details","Films", new {film.FilmId})">@film.Title</a>"
        </li>
    }
</ul>